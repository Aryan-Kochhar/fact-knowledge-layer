# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# dist is deliberately NOT ignored. The backend serves it directly, so a
# reviewer needs only Python to run the whole app - no Node, no build step.
# Rebuild with `npm run build` after changing anything under src/.
dist-ssr
*.local

# Static-data bundle, generated; not part of the app.
public/data

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
